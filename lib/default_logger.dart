library logger.default_logger;

/// The package shares a singleton logger used to quick start.
///
/// Use it only for quick tasks as it's always a good idea to create
/// a new [Logger] instance rather than use the default logger.

export "src/default_logger/default_logger.dart";
